# Optional backend API URL for split local development only.
# Leave empty when Flask serves the exported frontend on port 7860; requests
# will then stay same-origin and work with either localhost or 127.0.0.1.
NEXT_PUBLIC_API_URL=
