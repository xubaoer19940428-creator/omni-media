# Shared UI primitives

This Flask application has no local component library or reusable component directory.
Bootstrap 5.1.3 supplies the external button, input, alert, progress, grid, and utility primitives.
The only page-specific markup lives in `templates/index.html`, which must be passed in full for design work.

