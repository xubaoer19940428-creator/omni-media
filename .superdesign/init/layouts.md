# Shared layouts

There is no separate shared layout component. The single page shell, sidebar, header, form, result card, progress state, and completion state are all implemented directly in:

- `templates/index.html` — full page and layout source

Design work for the home page must pass that file in full.

